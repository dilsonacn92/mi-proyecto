name: CI con dos jobs

on:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Paso de verificación
        run: echo "Todo esta ok"

  deploy:
    runs-on: ubuntu-latest
    needs: check
    steps:
      - name: Deploy simulado
        run: echo "Deploy ejecutado"
